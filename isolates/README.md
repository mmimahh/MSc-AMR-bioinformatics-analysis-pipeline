This folder is filled in automatically by `scripts/00_make_isolate_lists.sh`.
You don't need to put anything here yourself — just make sure the FASTA
folder paths in `config.sh` are correct, then run:

```bash
bash scripts/00_make_isolate_lists.sh
```
